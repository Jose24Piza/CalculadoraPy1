name: CI - Tests Automatizados

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]
    
    steps:
    - name: 📥 Checkout del repositorio
      uses: actions/checkout@v3
    
    - name: 🐍 Configurar Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: 📦 Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov
    
    - name: 🧪 Ejecutar pruebas con pytest
      run: |
        # ✅ SOLUCIÓN: Agregar el directorio actual al PYTHONPATH
        PYTHONPATH="${PYTHONPATH}:${PWD}" pytest tests/ -v --cov=. --cov-report=term-missing
    
    - name: 📊 Generar reporte de cobertura
      run: |
        PYTHONPATH="${PYTHONPATH}:${PWD}" pytest tests/ --cov=. --cov-report=html
    
    - name: 📤 Subir reporte de cobertura
      uses: actions/upload-artifact@v3
      with:
        name: coverage-report
        path: htmlcov/